<div class="form-group row custom-field" data-sort="{{ custom_field.sort_order }}">
  <label class="col-sm-2 col-form-label" for="input-custom-field{{ custom_field.custom_field_id }}">{{ custom_field.name }}</label>
  <div class="col-sm-10">
    <select name="custom_field[{{ custom_field.custom_field_id }}][-1]" id="input-custom-field{{ custom_field.custom_field_id }}" class="form-control">
      <option value="">{{ text_none }}</option>
    {% for listing_item in custom_field.listing_items %}
      {% if listing_item.listing_item_id in custom_field.value[-1] %}
        <option value="{{ listing_item.listing_item_id }}" selected="selected">{{ listing_item.name }}</option>
      {% else %}
        <option value="{{ listing_item.listing_item_id }}">{{ listing_item.name }}</option>
      {% endif %}
    {% endfor %}
    </select>
    {% if error_custom_field[custom_field.custom_field_id] %}
      <div class="invalid-tooltip">{{ error_custom_field[custom_field.custom_field_id] }}</div>
    {% endif %}
  </div>
</div>